Existing Bugs:
--------------
1) It is running fine for the two programs listed here (in directory programs/), but not yet sure if it will run fine for everything.
2) Have not yet tested with difftest.pl
